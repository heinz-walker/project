<!-- 스크린샷/GIF를 넣고 싶으면 여기에 두고 상위 README.md에서 참조한다(선택). -->

# assets

상위 `README.md`에서 참조하는 화면 자료.

| 파일 | 내용 | 상태 |
|---|---|---|
| `01-overview.gif` | 메인 화면 → 챕터 아코디언 → 사회공학 챕터 훑어보기 (520px, ~2.6MB) | 있음 |

추가 화면(피싱 클릭 시뮬레이션, 비밀번호 강도 측정기 등)을 넣고 싶으면 여기에 GIF를 두고 `README.md`에서 `![캡션](assets/파일명)`으로 참조한다.

GIF 권장: 폭 약 800px, 5MB 이하. 맥에서 `Cmd+Shift+5` 화면 녹화 후 Kap/Gifox/CleanShot로 GIF 내보내기.
GitHub 마크다운은 GIF를 자동 재생·반복한다.
